INCLUDE_DIRECTORIES(${INCLUDE_DIR})

ADD_DEFINITIONS(-D_UNICODE)
ADD_DEFINITIONS(-DMZ_EXPORT_LIB)

FILE(GLOB METAZION_HEADERS "./*.hpp")
FILE(GLOB METAZION_SOURCES "./*.cpp")

ADD_SUBDIRECTORY(Share)
ADD_SUBDIRECTORY(Net)